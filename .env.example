# Directory that contains kubeconfig (the file inside must be named "config").
# Linux / WSL home:
# KUBECONFIG_HOST_DIR=/home/YOUR_USER/.kube
# Windows kubeconfig from WSL:
# KUBECONFIG_HOST_DIR=/mnt/c/Users/YOUR_WINDOWS_USER/.kube

# Optional: always include demo clusters
# DEMO_MODE=true
